
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

source = ~/.config/hypr/keybinds.conf

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = eww daemon & eww open bar & hyprpaper --config ~/.config/hypr/kitr/hyperpaper.conf & vencord & steam


windowrulev2 = opacity 0.0 override 0.0 override,class:^(xwaylandvideobridge)$
windowrulev2 = nofocus,class:^(xwaylandvideobridge)$
windowrulev2 = noinitialfocus,class:^(xwaylandvideobridge)$


# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 15
    gaps_out = 10
    border_size = 2
    col.active_border =   rgba(ff6699ee) rgba(dd00ffee) 45deg
   col.inactive_border = rgba(777777ee) rgba(777777ee) 315deg

    layout = dwindle
}

decoration {
    drop_shadow = yes
    rounding = 2
    shadow_range = 16
    shadow_render_power = 3
    col.shadow = rgba(110911ee)

    blur {
        size = 1
        passes = 3
    }

}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.0

    bezier = myEaseInOutF, 0.5, 0.2, 0.5, 0.5

    bezier = myEaseOutIn, 0.0, 0.8, 1.0, 0.3

    bezier = myWorkSpace, 0.9, 0.-0.1, 0.6, 1.0

    bezier = mySmol, 0.8, 0, 0.2, 1.0

    bezier = linear, 0, 0, 1, 1

    bezier = winOut, 0.6, -0.1, 1.0, 0.3

    bezier = winOutFade, 0.2, 0.9, 0.3, 0.9

    bezier = winIn, 0.3, 0.8, 0.2, 1.05

    bezier = benis, 0.3, 0.8, 0.2, 1.05

    animation = windows, 1, 7, myBezier, slide
    animation = windowsIn, 1, 7, winIn, slide
    animation = fadeIn, 1, 2, winIn
    animation = windowsOut, 1, 7, winOut, slide
    animation = fadeOut, 1, 2, winOutFade 

    animation = windowsMove, 1, 7, benis

    animation = fade, 1, 7, default
    
    animation = workspaces, 1, 3, myWorkSpace, slidevert

    animation = border, 1, 5, myEaseInOutF
    animation = borderangle, 1, 69, linear, loop
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
    
    use_active_for_splits = false
    
    no_gaps_when_only = false
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    always_center_master = false
    allow_small_split = false

    orientation = left

}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

windowrule=float,title:^(Picture-in-Picture)$


# Screenshots


#cliphest
exec-once = wl-paste --type text --watch cliphist store #Stores only text data

exec-once = wl-paste --type image --watch cliphist store #Stores only image data

#mako

exec-once = mako

#dbus

exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

exec-once=/usr/lib/polkit-kde-authentication-agent-1
